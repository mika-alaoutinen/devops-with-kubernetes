apiVersion: v1
kind: Service
metadata:
  name: todo-frontend-svc
spec:
  type: NodePort
  selector:
    app: todo-frontend
  ports:
    - name: http
      nodePort: 30001
      protocol: TCP
      port: 8080
      targetPort: 3000